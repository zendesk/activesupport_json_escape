source "https://rubygems.org"

gemspec

if (activesupport_version = ENV["ACTIVESUPPORT"])
  gem "activesupport", activesupport_version
end

if (json_version = ENV["JSON_VERSION"])
  gem "json", json_version
end

gem "benchmark-ips"
gem "minitest", "~> 6.0"
gem "rake", "~> 13.0"
gem "rake-compiler", "~> 1.0"
gem "ruby_memcheck"
gem "standard"
